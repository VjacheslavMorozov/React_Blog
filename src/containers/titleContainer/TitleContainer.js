import React from 'react';
import Title from "../../components/title/Title"

const TitileContainer = () => {
    return (
        <div>
            <Title />
        </div>
    );
};

export default TitileContainer;
